[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
addopts = -p no:doctest
markers =
    paraphrase: Phase 3 YAML paraphrase synonym regressions (rules path)
    llm: Phase 3 live LLM regressions (requires RUN_LLM_TESTS=1 and LLM_API_KEY)
